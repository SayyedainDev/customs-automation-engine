fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart
aiofiles
SQLAlchemy>=2.0,<2.1
psycopg[binary]>=3.2,<4
pytest
httpx
beautifulsoup4>=4.12,<5
langchain-community>=0.4,<0.5
pymupdf>=1.24,<2
groq>=0.31,<1
numpy>=1.26,<3
scikit-learn>=1.3,<2
# Phase 3B production RAG. Optional at runtime: when absent, the regulatory
# module runs in explicitly labelled degraded mode (hashing embeddings + lexical
# reranking). Install these to enable real dense embeddings and cross-encoder
# reranking. RAGAS is optional and only used by the evaluation script.
sentence-transformers>=2.6,<4
# ragas>=0.1  # optional; enable RAG-answer metrics in scripts/evaluate_production_rag.py
# Phase 3C: LangGraph multi-agent orchestration + human-in-the-loop.
langgraph>=1.0,<2
langgraph-checkpoint-sqlite>=3,<4
langgraph-checkpoint-postgres>=3,<4
