# Synthetic Customs Test Factory

This directory contains fictional, generated documents for testing only.
Nothing here is a real shipment, company, or government filing.

The repository currently contains 192 unique synthetic PDFs: 15 primary
shipment scenarios, 21 supporting-document bundles, text and image-only
variants, plus the original legacy demonstration bundle.

## Original hand-built bundle

`synthetic_customs_test_bundle/` is the original single-scenario bundle (clean single-line cotton T-shirt shipment to China). It is unchanged by this factory.

## Generated scenario matrix

Every folder below was produced by
`backend/scripts/generate_synthetic_test_bundles.py`. The generator reads the
five curated PCT records and product requirements from `regulatory_data/`.
Scenario fields are hand-authored fictional ground truth.

`expected_extraction_and_checks.json` includes a convenient pipeline-derived
snapshot for inspecting individual folders; it is not used as an independent
oracle for the same code that produced it. Independent grading uses
`scenario_manifest.json`, whose expected statuses are hand-declared without
importing the compliance engine or item matcher.

Supporting-document bundles under `supporting_documents/` are generated by
`backend/scripts/generate_supporting_documents.py`.

Each folder contains:

- `synthetic_commercial_invoice_text.pdf` / `synthetic_packing_list_text.pdf` - selectable-text PDFs.
- `synthetic_commercial_invoice_scanned.pdf` / `synthetic_packing_list_scanned.pdf` - image-only rasterized PDFs with no embedded text, to exercise the Tesseract OCR fallback path.
- `expected_extraction_and_checks.json` - ground-truth shipment data plus the computed expected status of every check.
- `multi_line_api_request.json` - a ready-to-use request body for `POST /api/v1/compliance/check-documents/multi-line` (or the customs-audit workflow), with the document-ID placeholders left for you to fill in after uploading.

| Folder | PCT code(s) | Scenario | Expected overall status |
| --- | --- | --- | --- |
| `clean_raw_cotton/` | 5201.0090 | Clean raw-cotton shipment (all SRO 2486(I)/2025 conditions met) | `passed` |
| `clean_cotton_yarn/` | 5205.1100 | Clean cotton-yarn shipment to China | `passed` |
| `clean_denim_fabric/` | 5209.4200 | Clean denim-fabric shipment to China | `passed` |
| `clean_cotton_tshirts/` | 6109.1000 | Clean cotton knitted T-shirt shipment to China | `passed` |
| `clean_bedsheets/` | 6302.3110 | Clean cotton bed-sheet shipment to China | `passed` |
| `single_line_declared_total_fallback/` | 6109.1000 | Single-line T-shirt invoice with weights only as a document total | `passed` |
| `multi_line_shipment/` | 5209.4200, 6109.1000, 6302.3110 | Multi-line shipment: T-shirts, denim and bed sheets to China | `passed` |
| `non_china_destination_coo/` | 5205.1100 | Cotton-yarn shipment to Germany (non-CPFTA certificate of origin) | `passed` |
| `error_missing_form_e/` | 6109.1000 | Missing Form-E | `failed` |
| `error_quantity_mismatch/` | 6109.1000 | Invoice/packing-list quantity mismatch | `failed` |
| `error_arithmetic_mismatch/` | 6109.1000 | Quantity times unit price does not equal the declared line total | `failed` |
| `error_weight_inversion/` | 6109.1000 | Gross weight lower than net weight | `failed` |
| `error_raw_cotton_missing_sbp_deposit/` | 5201.0090 | Raw cotton shipment missing the SBP deposit proof | `failed` |
| `error_raw_cotton_deadline_exceeded/` | 5201.0090 | Raw cotton shipped more than 180 days after the letter of credit | `failed` |
| `manual_review_unsupported_pct_code/` | 9999.9999 | PCT code outside the validated MVP catalog | `manual_review` |

## Running a scenario through the workflow

```bash
cd backend
python -m scripts.run_synthetic_folder_workflow ../synthetic_factory/<scenario_key>
```

Or point it at the whole `synthetic_factory/` directory to run every scenario (original bundle plus the generated matrix) in one pass; the runner discovers each `_text`/`_scanned` pair per folder automatically.

## Regenerating

```bash
cd backend
python -m scripts.generate_synthetic_test_bundles
```

All names, addresses, invoice numbers, prices, and shipment details are fictional.
